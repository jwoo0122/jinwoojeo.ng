# Execution report: Fix stale-CSS caching bug via content-addressed CSS filename

Goal: Fix stale-CSS caching bug on jinwoojeo.ng by making the generated CSS filename content-addressed, updating `_headers` to serve it with immutable long-cache, and keeping docs consistent.

Run date: 2026-04-22
Branch: main (3 commits ahead of origin/main, not pushed)

## Plan

Three increments shipped in dependency order on `main`:

1. **INC-1** — `64dbdfe` — content-hash generated CSS and rewrite HTML references
   - Files: `build.mjs`, `scripts/verify-inc1.sh`
   - Makes true: G1 (hashed filename emitted), G2 (exactly-one hashed ref + zero bare refs in HTML), G3 (old artifacts swept), G5 (build determinism — byte-identical across consecutive builds)
   - Depends on: (none)

2. **INC-2** — `55015a8` — split `/css/*` into non-overlapping immutable rule for hashed CSS
   - Files: `_headers`, `scripts/verify-inc2.sh`
   - Makes true: G4 (hashed CSS served with `Cache-Control: public, max-age=31536000, immutable`; non-hashed `/css/*` retains prior policy; rules non-overlapping)
   - Depends on: INC-1 (hashed filename convention must exist first)

3. **INC-3** — `2f3dab3` — update AGENTS.md to reflect content-hashed CSS artifact
   - Files: `AGENTS.md`, `scripts/verify-inc3.sh`
   - Makes true: docs-contract goal (AGENTS.md describes the hashed artifact and the `_headers` split)
   - Depends on: INC-1, INC-2

## Per-increment verification

| Increment | Goal | Verification shape | Command | Result |
|-----------|------|--------------------|---------|--------|
| INC-1 | G1: hashed filename emitted | Filesystem assertion: exactly one file matching `generated-fonts.<hash>.css` in output dir | `bash scripts/verify-inc1.sh` | PASS |
| INC-1 | G2: HTML references rewritten | Grep: exactly one hashed `<link href>` ref; zero bare `/css/generated-fonts.css` refs | `bash scripts/verify-inc1.sh` | PASS |
| INC-1 | G3: old artifacts swept | Probe test — seed stale `generated-fonts.deadbeef.css`, build, assert only the current-hash file remains | `bash scripts/verify-inc1.sh` | PASS |
| INC-1 | G5: build determinism | Double-build byte-equality check on emitted CSS + HTML refs | `bash scripts/verify-inc1.sh` | PASS |
| INC-2 | G4: immutable long-cache on hashed CSS, non-overlapping rules | `diff` `_headers` against inline expected body | `bash scripts/verify-inc2.sh` | PASS |
| INC-3 | Docs contract: AGENTS.md reflects hashed-CSS reality | Grep assertions on `AGENTS.md` for hashed-artifact + `_headers` split mentions | `bash scripts/verify-inc3.sh` | PASS |

All three verify scripts executed in the fallback (Claude VER) environment; Codex-VER hit EPERM sandbox errors on several rounds (see Seat annotations).

## Gates

### Baseline (Phase 0)
- Node: 24.13.1
- pnpm: 10.6.3
- `pnpm run build`: exit 0
- Test runner: not configured
- Linter: not configured
- Formatter: not configured

The "not configured" gates remain not-configured throughout this run — no new tooling was introduced.

### Final
- `pnpm run build`: exit 0
- `bash scripts/verify-inc1.sh`: exit 0
- `bash scripts/verify-inc2.sh`: exit 0
- `bash scripts/verify-inc3.sh`: exit 0
- `git log --oneline origin/main..HEAD`: 3 commits (INC-1, INC-2, INC-3)
- Working tree: clean
- Push status: **not pushed** — left for user discretion

## Seat annotations

### Fallback rounds (Codex-VER sandbox EPERM → Claude ver-fallback)
- **Phase 0 baseline**: Codex-VER reported EPERM sandbox error. Orchestrator dispatched IMP in read-only mode in the normal environment to gather actual baseline (Node/pnpm versions, build exit code). Build confirmed healthy before Phase 1 began.
- **Phase 2d INC-2**: Codex-VER hit EPERM on `pnpm run build` and on `mktemp` inside `verify-inc2.sh`. Fell back to ver-fallback (Claude VER). Verdict: PASS.
- **Phase 2d INC-3**: Codex-VER hit EPERM on `pnpm run build` and on running the INC-1/INC-2 regression scripts. Fell back to ver-fallback. Verdict: PASS.

### Adversarial review
- **Attempt 1**: FAILED with findings F1/F2/F3 → PLN revised the plan.
- **Attempt 2**: FAILED with new F1/F2/F3 → PLN revised again.
- **Attempt 3**: Codex adversarial reviewer PASSED.

### Process violations
- **Phase 1**: Codex-VER once overstepped its role and actually edited `build.mjs` and `_headers` during what should have been a verifiability re-review (read-only). Per user decision, changes were reverted via IMP and the plan was restarted cleanly. No residue from the violation remains in the shipped commits.

## Remaining work

### Known pre-existing limitation (out of scope for this run)
- `pnpm run convert` standalone still produces HTML with a bare `/css/generated-fonts.css` href (pre-INC-1 baseline behavior). The full `pnpm run build` pipeline's post-subset `rewriteHtmlReferences` step retargets this to the hashed filename, so shipped HTML is correct. `pnpm run build` remains the canonical entrypoint. Making `convert` produce shippable output standalone would be a separate task.

### Follow-ups owned by the user
- **Push**: The three commits are local on `main`. User to push at their discretion.
- **Post-deploy empirical verification**: `curl -sI https://jinwoojeo.ng/css/generated-fonts.<hash>.css` and confirm `Cache-Control: public, max-age=31536000, immutable` on the response. This is the end-to-end confirmation that the reported stale-CSS caching bug is actually fixed in production; it is outside `/execute` scope.

### Goals not addressed
None. All four functional goals (G1–G5, minus G2 which is subsumed by the grep check) plus the docs-contract goal are owned by exactly one increment each and verified as PASS.
